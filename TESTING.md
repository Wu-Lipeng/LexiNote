# 验证记录

验证日期：2026-09-15。插件版本：0.1.1。

## Windows 桌面集成测试

在独立配置和临时文献库中运行，词典请求使用本机模拟 JSON 服务。下表为 `Zotero.version` 报告的实际运行版本。

| Zotero | 通过 / 总数 |
|---|---|
| 7.0.32 | 11 / 11 |
| 8.0.4 | 11 / 11 |
| 9.0.6 | 11 / 11 |
| 10.0.2 | 11 / 11 |

每个版本验证：

1. 插件加载、设置页注册、配置保存和凭据存储读取/删除。
2. 真实 HTTP GET / POST 请求，以及释义、音标和例句字段提取。
3. 多个单词并发保存、同一文献多个附件共用一条笔记、重复单词检查。
4. 保留笔记中已保存的手工编辑，并将接口 HTML 作为普通文字保存。
5. 浮窗显示、保存按钮及所属文献关联。
6. 快速换词移除旧浮窗，关闭时清理。
7. 实际 Zotero 设置页加载、保存设置和测试接口按钮。
8. 导入测试 PDF，使用 Gecko 的鼠标事件模拟双击，经过阅读器自己的选词逻辑触发官方事件，自动查词并保存到父文献笔记。
9. 独立附件提示先创建父条目。
10. 生词本被放入回收站后创建新笔记。
11. 插件停止时取消请求、清理缓存和浮窗。

原始结果位于源码包 `tests/results/`。

## 其他自动测试

- 12 项 Node 核心测试：Unicode 单词筛选、JSON 转义、响应数组路径、配置校验、密钥要求、缓存失效、取消、超时、HTTP 错误、响应大小、并发上限、保存队列、LRU 容量和安装清单检查。
- 1 项浏览器 DOM 测试：包含 14 项界面/数据断言，以及设置保存和接口测试。该测试使用模拟 Zotero 数据层，桌面兼容性依据上面的真实应用集成测试。

## 验证边界

- 已实测 Windows 上列出的版本；没有验证所有历史补丁版，也没有验证 macOS / Linux。
- 已验证内置 PDF 阅读器。EPUB、网页快照、笔记编辑器和 Zotero 外部软件的划词暂不作为已验证功能。
- 未接入使用者尚未提供的真实词典服务。带动态签名、OAuth 或特殊响应格式的服务可能需要适配。
- 本轮验证了请求并发上限和缓存容量，没有进行十万条文献库的性能基准测试。插件启动不会扫描整库；保存时只查找所属文献的子笔记。
- 当前为手动分发版本，不提供在线自动更新。
